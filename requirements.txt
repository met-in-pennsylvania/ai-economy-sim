# Install the local package first — ensures ai_econ_sim is fully installed
# on Streamlit Cloud rather than relying solely on sys.path manipulation
.

# Runtime dependencies
numpy>=1.26
pandas>=2.1
pyyaml>=6.0
matplotlib>=3.8
plotly>=5.18
streamlit>=1.30
